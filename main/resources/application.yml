server:
  port: 8080

#默认HikariCP连接池
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/test

  redis:
    port: 6379
    host: localhost

#默认日志框架Logback
logging:
  level:
    com.example: debug